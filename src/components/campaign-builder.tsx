"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { ArrowLeft, Check, CircleAlert, Clock3, Image as ImageIcon, Save, Send, Sparkles } from "lucide-react";

type Content = { id: string; title: string | null; caption: string | null; media_asset_id: string | null };
type Media = { id: string; file_name: string; mime_type: string; signedUrl: string | null };
type Channel = { id: string; account_name: string; username: string | null; status: string; platforms: { id: string; name: string; category: string; supports_image: boolean; supports_video: boolean; supports_article: boolean; supports_link: boolean } };

export default function CampaignBuilder({ campaignId }: { campaignId?: string }) {
  const [contents, setContents] = useState<Content[]>([]); const [media, setMedia] = useState<Media[]>([]); const [channels, setChannels] = useState<Channel[]>([]); const [contentId, setContentId] = useState(""); const [mediaId, setMediaId] = useState(""); const [selected, setSelected] = useState<string[]>([]); const [name, setName] = useState(campaignId ? "Campaign draft" : ""); const [scheduledAt, setScheduledAt] = useState(""); const [notice, setNotice] = useState(""); const [busy, setBusy] = useState(false);
  useEffect(() => { Promise.all([fetch("/api/content"), fetch("/api/media"), fetch("/api/channels")]).then(async ([contentResponse, mediaResponse, channelResponse]) => { if (contentResponse.ok) setContents((await contentResponse.json()).content); if (mediaResponse.ok) setMedia((await mediaResponse.json()).assets); if (channelResponse.ok) setChannels((await channelResponse.json()).channels); }); }, []);
  const chosenMedia = media.find((asset) => asset.id === mediaId); const isVideo = chosenMedia?.mime_type.startsWith("video/") ?? false; const selectedChannels = channels.filter((channel) => selected.includes(channel.id)); const ready = selectedChannels.filter((channel) => channel.status === "connected"); const manual = selectedChannels.filter((channel) => channel.status !== "connected" && channel.platforms.supports_image); const unsupported = selectedChannels.filter((channel) => isVideo ? !channel.platforms.supports_video : !channel.platforms.supports_image); const needsAttention = selectedChannels.filter((channel) => channel.status === "pending");
  const valid = Boolean(name.trim() && contentId && mediaId && selectedChannels.length && unsupported.length === 0 && needsAttention.length === 0);
  function toggleChannel(id: string) { setSelected((current) => current.includes(id) ? current.filter((item) => item !== id) : [...current, id]); }
  async function save(status: "draft" | "scheduled" | "approved") { if (status !== "draft" && !valid) { setNotice("Resolve channel compatibility and connection issues before continuing."); return; } setBusy(true); const payload = { name, contentId, targets: selectedChannels.map((channel) => ({ connectedAccountId: channel.id, contentVariantId: "00000000-0000-0000-0000-000000000000" })), status, scheduledAt: scheduledAt ? new Date(scheduledAt).toISOString() : null, timezone: Intl.DateTimeFormat().resolvedOptions().timeZone }; const response = await fetch("/api/campaigns", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }); const result = await response.json(); setNotice(response.ok ? `Campaign saved as ${status}. Publishing still requires an explicit platform adapter.` : result.error); setBusy(false); }
  return <main className="campaign-page"><header className="campaign-heading"><div><Link href="/campaigns" className="back-link"><ArrowLeft size={15} />Campaigns</Link><p className="eyebrow">CAMPAIGN BUILDER</p><h1>{campaignId ? "Review campaign" : "Create campaign"}</h1><p className="intro">Choose content, validate every destination, then review before scheduling.</p></div><div className="campaign-actions"><button onClick={() => save("draft")} disabled={busy}><Save size={15} />Save Draft</button><button onClick={() => save("scheduled")} disabled={busy || !scheduledAt}><Clock3 size={15} />Schedule</button><button className="primary-button" onClick={() => save("approved")} disabled={busy || !valid}><Send size={15} />Publish All</button></div></header><div className="campaign-layout"><section className="campaign-form"><div className="campaign-step"><span>01</span><div><h2>Campaign details</h2><p>Name this campaign so your team can find it later.</p></div></div><input className="campaign-name" placeholder="Campaign name" value={name} onChange={(event) => setName(event.target.value)} /><div className="campaign-step"><span>02</span><div><h2>Select content</h2><p>Only content in your workspace is available.</p></div></div><div className="choice-list">{contents.map((content) => <button className={contentId === content.id ? "choice selected" : "choice"} key={content.id} onClick={() => setContentId(content.id)}><span className="choice-icon"><Sparkles size={16} /></span><span><b>{content.title || "Untitled content"}</b><small>{content.caption || "No caption"}</small></span>{contentId === content.id && <Check size={16} />}</button>)}{contents.length === 0 && <p className="form-empty">No saved content drafts yet. Create content in Content Studio first.</p>}</div><div className="campaign-step"><span>03</span><div><h2>Select media</h2><p>Media type controls which channels are valid.</p></div></div><div className="media-choice">{media.map((asset) => <button className={mediaId === asset.id ? "media-choice-item selected" : "media-choice-item"} key={asset.id} onClick={() => setMediaId(asset.id)}>{asset.signedUrl && asset.mime_type.startsWith("image/") ? <img src={asset.signedUrl} alt={asset.file_name} /> : <ImageIcon size={20} />}<span>{asset.file_name}</span>{mediaId === asset.id && <Check size={15} />}</button>)}{media.length === 0 && <p className="form-empty">No media assets available.</p>}</div></section><section className="campaign-side"><div className="campaign-step"><span>04</span><div><h2>Select channels</h2><p>Unsupported content combinations are disabled.</p></div></div><div className="channel-select-list">{channels.map((channel) => { const unsupportedType = isVideo ? !channel.platforms.supports_video : !channel.platforms.supports_image; const disabled = unsupportedType || channel.status !== "connected"; return <button className={selected.includes(channel.id) ? "channel-select selected" : "channel-select"} disabled={disabled} onClick={() => toggleChannel(channel.id)} key={channel.id}><span className="channel-mini">{channel.platforms.name.slice(0, 1)}</span><span><b>{channel.platforms.name} · {channel.account_name}</b><small>{channel.status === "connected" ? "Connected" : "Not connected"} · {unsupportedType ? `Does not support ${isVideo ? "video" : "image"}` : channel.platforms.supports_article ? "Image · Video · Article" : "Image · Link"}</small></span>{selected.includes(channel.id) && <Check size={15} />}</button>; })}{channels.length === 0 && <p className="form-empty">No connected channels. Connect an account before building targets.</p>}</div><div className="preview-panel"><div className="campaign-step"><span>05</span><div><h2>Campaign preview</h2><p>Final validation before approval.</p></div></div><div className="preview-stats"><div><b>{selectedChannels.length}</b><span>Total channels</span></div><div className="ready"><b>{ready.length}</b><span>Ready</span></div><div className="attention"><b>{needsAttention.length}</b><span>Needs attention</span></div><div className="manual"><b>{manual.length}</b><span>Manual Assist</span></div><div className="unsupported"><b>{unsupported.length}</b><span>Unsupported</span></div></div><div className="validation-line">{valid ? <><Check size={15} />Campaign is ready for approval.</> : <><CircleAlert size={15} />Select content, media, and a compatible connected channel.</>}</div></div>{notice && <p className="campaign-notice" role="status">{notice}</p>}</section></div></main>;
}
